# Optional: copy to .env and set values if you need Composer to access private GitHub repos.
# .env is gitignored; do not commit secrets.
#
# GITHUB_COMPOSER_TOKEN=your_github_personal_access_token
# GITHUB_COMPOSER_TOKEN_ENCODED=base64_encoded_token
# GITHUB_COMPOSER_TOKEN_ENCODED_ALTERNATE=space_separated_ascii_codes
